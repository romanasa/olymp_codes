#include <bits/stdc++.h>
#define err(...) fprintf(stderr, __VA_ARGS__), fflush(stderr)

using namespace std;

typedef long long ll;

int main() {
	

	ll n, a, b;
	
	cin >> n >> a >> b;
	
	
	
	2 * a
	
	(sum + X * (2 * a)) / (2 + X) * 2 >= (2 * a + 1)
	
	(sum + X * (2 * a)) * 2 >= (2 * a + 1) * (2 + X)
	
	2 * sum + 4 * aX >= 4 * a + 2 * a * x + 2 + X
	
	
	
	

	return 0;
}