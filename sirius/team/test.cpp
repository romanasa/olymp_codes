#include <bits/stdc++.h>
#define err(...) fprintf(stderr, __VA_ARGS__), fflush(stderr)

using namespace std;

typedef long long ll;

int main() {
#ifdef WIN
    freopen("input.txt", "r", stdin);
#endif
	return 0;
}
