#include <bits/stdc++.h>
#define err(...) fprintf(stderr, __VA_ARGS__), fflush(stderr)

using namespace std;

typedef long long ll;

int main() {
    int n;
    for(n = 1; n <= 100; ++n)
    {
        for(int j = 0; j <= 10000; ++j)
        {
            dp[1] = j;
            for(int k = 2; k <= n; ++k)
            {

            }
        }
    }
	return 0;
}
