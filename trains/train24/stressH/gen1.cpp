#include <bits/stdc++.h>
#define err(...) fprintf(stderr, __VA_ARGS__), fflush(stderr)

using namespace std;

typedef long long ll;

int main(int argc, char* argv[]) {
	srand(atoi(argv[1]));
	return 0;
}