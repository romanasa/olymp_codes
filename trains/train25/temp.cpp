#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

#define TASK ""

int main() {
#ifdef WIN
    freopen("01", "r", stdin);
#else
    freopen(TASK".in", "r", stdin);
    freopen(TASK".out", "w", stdout);
#endif // WIN
    return 0;
}

